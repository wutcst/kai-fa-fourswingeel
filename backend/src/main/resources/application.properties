server.port=8080
spring.application.name=slay-the-spire-clone    

# MySQL 连接（请根据实际用户名/密码修改）
spring.datasource.url=jdbc:mysql://localhost:3306/cardgame?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=sca5332428
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis-Plus 配置
mybatis-plus.mapper-locations=classpath*:/mapper/**/*.xml
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# Redis 配置（本地默认）
spring.data.redis.host=localhost
spring.data.redis.port=6379